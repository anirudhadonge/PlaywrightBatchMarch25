/**
 * Locator: Address of a element in the html page.
 * id
 * class
 * Xpath
 * Css locator
 * 
 * 
 */

/**
 * Xpath
 * syntax ://<TagName>[@<Attribute>='<AttributeValue>']
 * Text:  //<TagName>[text()='<Label>']
 * contains: It used when you have a partial value of Text , AttributeValue
 *   //<TagName>[contains(@<attributeName,'<AttributeValue>')]
 *  * and : //<TagName>[@<Attribute1>='<AttributeValue' and @<Attribute2>='AttributeValue']
 * 
 * 1. //Parent: 
 * 2. //Ancestor::<TagName>
 * 3. Following-sibling::<TagName>
 * 4. Preceeding-Sibling::<TagName>
 * 
 * Developer Options
 * 1. F12
 * 2. Right click on the element and select Inspect
 */